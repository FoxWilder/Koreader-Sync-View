# This workflow has been superseded by build.yml.
# Releases are now created automatically on every push to main —
# no manual tagging required. See build.yml for the current process.
#
# This file is kept to avoid breaking existing bookmarks/links but has
# no automatic triggers and does nothing useful.

name: (Deprecated) Manual Release

on:
  workflow_dispatch:
    inputs:
      note:
        description: >
          This workflow is deprecated. Releases are now automatic on push to
          main. See .github/workflows/build.yml.
        required: false

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated."
          echo "Releases are created automatically on every push to main."
          echo "See .github/workflows/build.yml for the current release process."
